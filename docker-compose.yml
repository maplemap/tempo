services:
  app:
    build: .
    container_name: tempo
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/data
